import React from 'react';
import { storiesOf } from '@storybook/react';
import GradientContainer from './GradientContainer';

storiesOf('GradientContainer', module).add('Normal', () => (
  <GradientContainer />
));
